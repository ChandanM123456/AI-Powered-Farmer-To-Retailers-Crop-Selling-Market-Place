CREATE TABLE IF NOT EXISTS crops (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    product_name TEXT,
    image_path TEXT,
    price REAL,
    ai_rating REAL,
    farmer_name TEXT,
    location TEXT,
    quantity REAL,
    quantity_unit TEXT
);
data = json.loads(post_data)
print(data)